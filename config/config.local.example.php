<?php
/**
 * Copiar este archivo como config.local.php (misma carpeta config/).
 * Ahí defines constantes solo para tu máquina; tiene prioridad sobre variables de entorno.
 *
 * Úsalo en XAMPP si por algún motivo tu sistema exporta DB_HOST u otras variables
 * (por ejemplo de Docker) y la app intenta conectarse mal.
 *
 * En un XAMPP típico NO hace falta: basta con borrar/renombrar .env de Docker
 * y Apache usará los valores por defecto de config.php (localhost, /marina, root, etc.).
 */

// define('MARINA_URL', '/marina');
// define('DB_HOST', '127.0.0.1');
// define('DB_NAME', 'marina');
// define('DB_USER', 'root');
// define('DB_PASS', '');
// define('DB_CHARSET', 'utf8mb4');
